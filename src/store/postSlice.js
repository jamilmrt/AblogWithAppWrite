import React from 'react'
import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    status: false,
    userData: null
}

function postSlice() {
  return (
    <div>
      
    </div>
  )
}

export default postSlice
